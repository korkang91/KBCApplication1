<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kangbc.kbcapplication1">

    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Camera를 사용한다고 사용자에게 알린다. -->
    <uses-feature android:name="android.hardware.camera2" />
    <!-- 찍힌 사진을 외장 모메리에 저장하기 위해서 외장 USB에 쓰지 권한을 요청한다. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity" />

        <activity android:name=".activity.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.kangbc.kbcapplication1.activity.DataBindingActivity"/>
        <activity android:name="com.kangbc.kbcapplication1.activity.CameraActivity"/>

    </application>

</manifest>